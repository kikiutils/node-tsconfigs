{
  "name": "@kikiutils/tsconfigs",
  "type": "module",
  "version": "5.0.0",
  "description": "Preconfigured TypeScript config files for stricter type checking and multi-environment support.",
  "author": "kiki-kanri",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kikiutils/node-tsconfigs.git"
  },
  "keywords": [
    "bun",
    "config",
    "module-target",
    "strict-mode",
    "ts",
    "tsconfig",
    "tsconfig-base",
    "tsconfig-extends",
    "tsconfig-variants",
    "typescript",
    "typescript-config",
    "typescript-settings"
  ],
  "scripts": {
    "bumplog": "changelogen --bump --hideAuthorEmail",
    "generate": "node --experimental-strip-types ./generate.ts",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "release": "pnpm run lint && pnpm run type-check && pnpm run generate && changelogen --hideAuthorEmail --push --release && sh ./generate-and-publish.sh",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@kikiutils/changelogen": "^0.8.0",
    "@kikiutils/eslint-config": "^0.12.2",
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
